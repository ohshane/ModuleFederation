server {
    # location ^~ /ui/v1/remote/__vite/ws {
    #   proxy_pass http://remote:2000/__vite/ws;
    #   proxy_http_version 1.1;
    #   proxy_set_header Upgrade $http_upgrade;
    #   proxy_set_header Connection "upgrade";
    #   proxy_read_timeout 1h;
    #   proxy_send_timeout 1h;
    #   proxy_buffering off;
    # }

    # location ^~ /ui/v1/host/__vite/ws {
    #   proxy_pass http://host:2000/__vite/ws;
    #   proxy_http_version 1.1;
    #   proxy_set_header Upgrade $http_upgrade;
    #   proxy_set_header Connection "upgrade";
    #   proxy_read_timeout 1h;
    #   proxy_send_timeout 1h;
    #   proxy_buffering off;
    # }

    location /ui/v1/host {
        proxy_pass http://host:2000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    location /ui/v1/remote {
        proxy_pass http://remote:2000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
